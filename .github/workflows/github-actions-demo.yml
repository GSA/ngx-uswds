name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      # - run: git remote add github "https://$cwolf10:${{secrets.COMMIT_REPO_TOKEN}}@github.com/${{ github.repository }}.git"
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      - run: npm i
      # - run: npm run ghpages
      - run: npm run build-storybook
      # - run:
      - run: touch storybook-static/.nojekyll
      - name: Prepare and deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx angular-cli-ghpages --dir=storybook-static

        # ng add angular-cli-ghpages
        #npm run ng -- deploy --name="cwolf10" --email="cameron.wolf@gsa.gov" --dir=storybook-static --base-href=/GSA/ngx-uswds/
      # - run: npx angular-cli-ghpages --dir=storybook-static
          # ng deploy --base-href=/GSA/ngx-uswds/
        # env:
        #   GITHUB_TOKEN:
        #     ${{secrets.COMMIT_REPO_TOKEN}}
      # - run:
